const Navbar = () => {

    return (
        <nav>
            <ul>
                <a href="#">Home</a>
                <a href="#">contact</a>
            </ul>
        </nav>
    )
}

export default Navbar;